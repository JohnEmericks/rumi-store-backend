# =============================================================================
# RUMI Backend Configuration
# =============================================================================
# 
# ⚠️  SECURITY WARNING:
#     1. Copy this file to .env (which is gitignored)
#     2. NEVER commit .env with real values to git
#     3. NEVER share your API keys
#
# =============================================================================

# Server
PORT=4000

# Database (PostgreSQL)
DATABASE_URL=postgres://username:password@localhost:5432/rumi

# OpenAI API Key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Admin Secret Key (for managing license keys)
# Generate a secure one with: openssl rand -hex 32
RUMI_ADMIN_SECRET=change-this-to-a-secure-random-string
